---
description: >-
  Dual-agent responsive design audit — Claude and Codex review independently
  for mobile/tablet issues, synthesize, devil's advocate pass, output as
  findings report
disable-model-invocation: true
---

Invoke the `audit-responsive` subagent via the Agent tool. Pass the scope from `$ARGUMENTS` as the prompt (or pass "current working directory" if `$ARGUMENTS` is empty).
